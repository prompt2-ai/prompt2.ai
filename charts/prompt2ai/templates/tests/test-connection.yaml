apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prompt2ai.fullname" . }}-test-connection"
  labels:
    {{- include "prompt2ai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prompt2ai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
